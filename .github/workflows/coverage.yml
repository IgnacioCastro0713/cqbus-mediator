name: coverage

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  coverage:
    runs-on: ubuntu-latest

    name: Code Coverage

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          coverage: xdebug

      - name: Install dependencies
        run: |
          composer require "laravel/framework:11.*" "orchestra/testbench:9.*" --no-interaction --no-update --dev
          composer update --prefer-stable --prefer-dist --no-interaction --with-all-dependencies --no-audit

      - name: Run tests with coverage
        run: vendor/bin/pest --coverage-clover coverage.xml --min=95

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          slug: IgnacioCastro0713/cqbus-mediator
          fail_ci_if_error: false
          verbose: true

